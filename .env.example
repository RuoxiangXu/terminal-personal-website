# Environment variables for terminal-personal-website
# Copy this file to `.env.local` and update the values.

# Public URL pointing to your resume PDF or hosted document.
# Used by the /resume route and the `resume` command as a fallback to siteConfig.resumeUrl.
NEXT_PUBLIC_RESUME_URL=
